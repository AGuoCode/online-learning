package com.guai.onlinelearning.utilities;

public class GenerateOTP {

    public static String generateOTPCode(int length) {
        // TODO I will currently use default otpcode before implemnt Email Service
        return "123456";
//        final String otpCharacter = "0123456789";
//        final StringBuilder otpBuilder = new StringBuilder();
//
//        SecureRandom random = new SecureRandom();
//
//        for (int i = 0; i < length; i++) {
//            int randomIndex = random.nextInt(otpCharacter.length());
//            otpBuilder.append(otpCharacter.charAt(randomIndex));
//        }
//        return otpBuilder.toString();
    }
}
